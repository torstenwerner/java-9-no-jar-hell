plugins {
    id 'java'
}

jar {
    baseName 'com.greetings'
}

// Run the code without Layers for testing purposes.
task simpleRun(type: Exec) {
    dependsOn 'jar'
    commandLine 'java', '-modulepath', 'build/libs', '-m', 'com.greetings/com.greetings.Main'
}
